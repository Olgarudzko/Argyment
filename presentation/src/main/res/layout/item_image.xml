<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:myapp="http://schemas.android.com/apk/res-auto">

    <data>
        <variable
            name="imageItem"
            type="by.argyment.gymapp.profile.MyPageImgItemViewModel" />
    </data>

    <LinearLayout
        android:layout_width="@dimen/preview_width"
        android:layout_height="@dimen/base_button_width"
        android:layout_margin="@dimen/corners_radius"
        android:background="@color/colorPrimaryDark"
        android:orientation="vertical">

        <ImageView
            android:id="@+id/my_pic"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            myapp:set_image="@{imageItem.picture}" />
    </LinearLayout>
</layout>